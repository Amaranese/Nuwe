import { AttendantsPipe } from './attendants.pipe';

describe('AttendantsPipe', () => {
  it('create an instance', () => {
    const pipe = new AttendantsPipe();
    expect(pipe).toBeTruthy();
  });
});
