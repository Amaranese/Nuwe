import { DestinationsPipe } from './destinations.pipe';

describe('DestinationsPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationsPipe();
    expect(pipe).toBeTruthy();
  });
});
