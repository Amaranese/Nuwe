import { VehicleTypePipe } from './vehicle-type.pipe';

describe('VehicleTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleTypePipe();
    expect(pipe).toBeTruthy();
  });
});
