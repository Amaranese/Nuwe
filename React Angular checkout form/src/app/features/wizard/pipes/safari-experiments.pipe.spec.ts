import { SafariExperimentsPipe } from './safari-experiments.pipe';

describe('SafariExperimentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SafariExperimentsPipe();
    expect(pipe).toBeTruthy();
  });
});
