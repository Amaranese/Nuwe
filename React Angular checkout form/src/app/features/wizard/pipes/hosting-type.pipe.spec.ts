import { HostingTypePipe } from './hosting-type.pipe';

describe('HostingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new HostingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
