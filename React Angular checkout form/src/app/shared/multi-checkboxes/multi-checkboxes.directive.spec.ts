import { MultiCheckboxesDirective } from './multi-checkboxes.directive';

describe('MultiCheckboxesDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiCheckboxesDirective();
    expect(directive).toBeTruthy();
  });
});
